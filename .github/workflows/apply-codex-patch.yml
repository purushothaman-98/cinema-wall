name: Apply Codex patch

on:
  push:
    branches: [main]
    paths:
      - ".codex-patches/zero-video-ui.patch"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Apply patch
        run: |
          git config user.name "codex-patch-applier"
          git config user.email "actions@users.noreply.github.com"
          git apply --check .codex-patches/zero-video-ui.patch
          git apply .codex-patches/zero-video-ui.patch
          python -m py_compile app.py scanner.py collectors.py data.py youtube_analysis.py
          rm -f .codex-patches/zero-video-ui.patch .github/workflows/apply-codex-patch.yml
          git add app.py .codex-patches/zero-video-ui.patch .github/workflows/apply-codex-patch.yml
          git commit -m "Separate catalog-only films from evidence-backed radar"
          git pull --rebase
          git push
